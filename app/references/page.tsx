import { ReferenceFormatter } from "@/components/ReferenceFormatter";

export const metadata = {
	title: "Reference Formatter",
	description: "Format academic citations and bibliographies in APA, MLA, Chicago, and Harvard styles.",
};

export default function ReferencesPage() {
	return <ReferenceFormatter />;
}
