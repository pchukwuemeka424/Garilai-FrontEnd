import { ReferenceFormatter } from "@/components/ReferenceFormatter";

export const metadata = {
	title: "References",
	description:
		"Format academic citations, build bibliographies, and validate references before submission.",
};

export default function ReferencesPage() {
	return <ReferenceFormatter />;
}
