import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { Card, CardContent } from "@/components/portal/ui/card";
export function StatMetric({ label, value, trend, icon: Icon }: { label: string; value: string; trend?: string; icon?: React.ElementType }) { const up = !trend?.startsWith("-"); return <Card><CardContent className="p-5"><div className="flex justify-between"><p className="text-sm font-medium text-foreground/60">{label}</p>{Icon && <Icon className="size-4 text-primary" />}</div><p className="mt-3 text-2xl font-bold tracking-tight">{value}</p>{trend && <p className={`mt-2 flex items-center gap-1 text-xs font-semibold ${up ? "text-success" : "text-danger"}`}>{up ? <ArrowUpRight className="size-3" /> : <ArrowDownRight className="size-3" />}{trend} vs. last month</p>}</CardContent></Card>; }
